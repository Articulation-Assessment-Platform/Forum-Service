version: '3.4'

services:
  forumservice:
    image: ${DOCKER_REGISTRY-}forumapi
    networks:
        - backend
    build:
      context: .
      dockerfile: ForumAPI/Dockerfile

networks:
  backend:
    name: backend
    external: true
    driver: bridge